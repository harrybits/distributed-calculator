# Values that work in a dev environment.
ops.can.do.all=true
# Override to localhost so unit tests with HTTP calls work.
ops.service.add.name=localhost
ops.service.subtract.name=localhost
ops.service.divide.name=localhost
ops.service.multiply.name=localhost
ops.service.power.name=localhost

logging.level.com.github.harrybits.dcalc.WebCalculatorVisitor=ERROR